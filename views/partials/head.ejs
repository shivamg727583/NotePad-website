<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title> Notepad</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="/css/style.css">
</head>

<body class="bg-gray-100">
  <!-- Navbar -->
  <nav class="bg-blue-500 p-4">
    <div class="container mx-auto flex justify-between items-center">
      <div class="text-white font-bold text-xl">Notepad</div>
      <ul class="flex w-1/3 ">
      
          <li class="ml-4 flex justify-between w-full">
            <a href="/" class="text-white hover:text-gray-200">Home</a>
            <a href="/create" class="text-white hover:text-gray-200">Create a Note</a>
            <a href="/notes" class="text-white hover:text-gray-200">View Notes</a>
            <a href="/signin" class="text-white hover:text-gray-200">Login</a>
            <a href="/signup" class="text-white hover:text-gray-200">Register</a>


          </li>
       
      </ul>

      <% if (locals.user) { %>
        <div class="text-white  text-xl flex w-1/4 justify-between"><h1 class="font-bold">Welcome, <%= user.name %></h1>
        <a href="/logout" class="text-red-400 hover:text-red-2700">Logout</a
        </div>
          <% } else { %>
            
                <% } %>
      
    </div>
  </nav>

  <div class="container mx-auto p-4">